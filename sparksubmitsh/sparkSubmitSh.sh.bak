#!/bin/sh

spark-submit \
  --master yarn \
  --deploy-mode cluster \
  --executor-memory 6g \
  --executor-cores 2 \
  --num-executors 50 \
  --driver-memory 10G \
  --files ${hiveConf} \
  --jars $SPARK_HOME/auxlib/Common-SerDe-1.0-SNAPSHOT.jar \
  --class mv.dp.ml.kylin.tags.InterestTagALL \
  "$jarfile" \
  $APP_ARGS